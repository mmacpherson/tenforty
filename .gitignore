# tenforty-specific
ots/generated/**
scratch/
SHARED_TASK_NOTES.md
.claude/*.json
.beads/
# This is a library, not an application.
uv.lock

# Benchmark outputs
bench_results.html

# tenforty-graph build outputs
crates/tenforty-graph/pkg/
crates/tenforty-graph/demo/pkg
crates/tenforty-graph/demo/graph.json

# tenforty-spec generated graph outputs (local dev artifacts)
tenforty-spec/*.json

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Cython output, regenerated from ots.pyx on every build — setup.py cythonizes
# unconditionally and Cython is a build requirement. Tracking it produced churn
# (a routine `uv sync` moving Cython 3.2.4 -> 3.2.9 rewrote 635 lines inside an
# unrelated PR) and risked worse: cythonize REUSES an existing .cpp when its
# version header matches the installed Cython and its mtime beats every
# dependency, so a committed .cpp can silently shadow the .pyx it came from.
# NOTE this is NOT ots_amalgamation.cpp, which stays tracked — the .pxd files do
# `cdef extern from "ots_amalgamation.cpp"`, making it a genuine build INPUT that
# would otherwise require the OTS release tarballs to reconstruct.
src/tenforty/otslib/ots.cpp

# Distribution / packaging
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*,cover
.hypothesis/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# IPython Notebook
.ipynb_checkpoints

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# dotenv
.env

# virtualenv
.venv/
venv/
ENV/

# Spyder project settings
.spyderproject

# Rope project settings
.ropeproject

# Dolt database files (added by bd init)
.dolt/
*.db

# Beads / Dolt files (added by bd init)
.beads-credential-key
